#!/usr/local/bin/chomik

variable update the persons:code;

let update the persons = value code
{
    let (X:person) can see (Y:person) = value boolean false;
    let (X:person) can hear (Y:person) = value boolean false;
    <update the relationship between persons (X:person) (Y:person)>;    
    <update the relationship between persons and functions (A:person) (F:function)>;
};

 
