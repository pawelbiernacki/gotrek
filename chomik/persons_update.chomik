#!/usr/local/bin/chomik

variable update the persons:code;

let variable update the persons = value code
{
    let variable (X:person) can see (Y:person) = value boolean false;
    let variable (X:person) can hear (Y:person) = value boolean false;
    execute <update the relationship between persons (X:person) (Y:person)>;
};

 
